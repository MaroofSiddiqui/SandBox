# Set your profile as the primary active profile
spring.profiles.active=dev-msaxena

# Include Mohit's profile configurations automatically
spring.profiles.include=dev-mohit

# Common Multipart Limits for Both Environments
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
